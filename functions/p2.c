// write a program to find factorial by using function with no argumrnt and return value
#include <stdio.h>
#include <math.h>
int sum();
int main()
{
    int num;
    num = sum();
    printf("\n sum of two given number= %d\n", num);
    return 0;
}
int sum()
{
    int a = 55, b = 60, sum;
    sum = sqrt(a) + sqrt(b);
    return (sum);
}
